defmodule Game.Board.CellTest do
  use ExUnit.Case
  alias Game.Board.Cell

  describe "Cell creation when" do
    test "only row and column provided" do
      cell = %Cell{}
    end

    test "all fields populated" do

    end
  end
end
